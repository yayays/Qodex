{
  "shapes": {
    "conversationRef": {
      "required": ["conversationKey", "platform", "scope", "externalId"],
      "optional": []
    },
    "senderRef": {
      "required": ["senderId"],
      "optional": ["displayName"]
    },
    "imageInput": {
      "required": ["url"],
      "optional": ["mimeType", "filename", "width", "height", "size"]
    },
    "conversationRecord": {
      "required": ["conversationKey", "platform", "scope", "externalId", "workspace", "backendKind", "createdAt", "updatedAt"],
      "optional": ["threadId"]
    },
    "pendingApprovalRecord": {
      "required": ["approvalId", "requestId", "conversationKey", "backendKind", "threadId", "turnId", "itemId", "kind", "payloadJson", "status", "createdAt"],
      "optional": ["reason"]
    },
    "messageLogRecord": {
      "required": ["role", "content", "createdAt"],
      "optional": ["threadId", "turnId"]
    }
  },
  "sendMessageParams": [
    "conversation",
    "sender",
    "text",
    "images",
    "workspace",
    "backendKind",
    "model",
    "modelProvider"
  ],
  "sendMessageResponse": [
    "accepted",
    "conversationKey",
    "threadId",
    "turnId"
  ],
  "approvalRequestedEvent": [
    "eventId",
    "approvalId",
    "conversationKey",
    "threadId",
    "turnId",
    "kind",
    "reason",
    "summary",
    "availableDecisions",
    "payloadJson"
  ],
  "conversationDetailsResponse": [
    "conversation",
    "runtime",
    "pendingApprovals",
    "recentMessages",
    "recentTurn",
    "recentError"
  ],
  "conversationRunningRuntime": [
    "threadId",
    "status",
    "activeFlags",
    "error"
  ],
  "pendingDeliveryRecord": [
    "eventId",
    "method",
    "conversationKey",
    "threadId",
    "turnId",
    "payloadJson",
    "createdAt"
  ]
}
